// @switchyomega/omega-pac — public API surface.
//
// Phase 0: stub. Phase 1 ports the real modules from the legacy CoffeeScript
// `omega-pac` package (conditions, profiles, rule_list, shexp_utils, utils, and
// a new `pac_ast` emitter replacing uglify-js). See docs/mv3-rewrite-plan.md §3.

/** Placeholder marker so the package has a real export before Phase 1. */
export const OMEGA_PAC_VERSION = '3.0.0'
