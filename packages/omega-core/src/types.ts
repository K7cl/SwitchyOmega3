import type { Profile } from '@switchyomega/omega-pac'

/** The full option set: profile keys ("+name"), settings ("-key"), schemaVersion. */
export type OmegaOptions = Record<string, unknown>

export type { Profile }
